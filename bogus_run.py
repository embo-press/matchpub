from pathlib import Path

if __name__ == "__main__":
    hi = "hello code ocean! I cannot build with docker-compose build or run with docker-compose run --rm matchpub python -m src.scan /data/test-data.xls /results/test-data"
    print(text)
    Path("./hi.txt").write_text(hi). # how can I put this in results/?
